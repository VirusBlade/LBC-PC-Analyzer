{
  "name": "lbc-minipc-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "d1:migrate:local": "wrangler d1 migrations apply lbc_minipc --local",
    "d1:migrate": "wrangler d1 migrations apply lbc_minipc --remote"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "wrangler": "^4.100.0"
  }
}
